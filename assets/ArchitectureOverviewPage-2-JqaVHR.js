import{D as o}from"./DocsNavFooter-Bt0ugVhp.js";import{D as a}from"./DocsArchDiagram-C8yhAGv0.js";import{D as l}from"./DocsPageHeader-DYolIm9t.js";import{u as d}from"./useDocHeadings-BOeQBIaq.js";import{d as p,k as h,c as u,e as n,a as t,j as i,o as c}from"./index-CCJa2PDF.js";import{_ as g}from"./_plugin-vue_export-helper-DlAUqK2U.js";const f={class:"docs-article"},y={class:"docs-content"},w=p({__name:"ArchitectureOverviewPage",setup(m){const{setHeadings:s}=d(),r=[{id:"high-level-architecture",text:"High-Level Architecture",level:2},{id:"staged-pipelines",text:"Staged Pipelines",level:2}];return h(()=>{s(r)}),(v,e)=>(c(),u("article",f,[n(l,{title:"Architecture Overview",icon:"bi bi-diagram-3"}),t("div",y,[e[0]||(e[0]=t("p",null,[i("Jeffrey consists of two applications: "),t("strong",null,"Jeffrey Microscope"),i(" (an analysis tool for visualizing and exploring JFR profiles) and "),t("strong",null,"Jeffrey Hub"),i(" (a recording collection service that manages workspaces, sessions, and live recordings). Jeffrey Microscope connects to Jeffrey Hub via gRPC to access remote workspaces, and also offers "),t("strong",null,"Recordings"),i(" for analyzing JFR files directly without a server connection.")],-1)),e[1]||(e[1]=t("h2",{id:"high-level-architecture"},"High-Level Architecture",-1)),e[2]||(e[2]=t("p",null,"Jeffrey runs as two separate applications. Jeffrey Hub collects and stores recordings from Java applications, while Jeffrey Microscope provides the analysis UI and connects to the server via gRPC:",-1)),n(a),e[3]||(e[3]=t("h2",{id:"staged-pipelines"},"Staged Pipelines",-1)),e[4]||(e[4]=t("p",null,"Some work in Microscope takes minutes rather than milliseconds — turning a recording into a profile, or initializing a heap dump. Those requests return immediately and the frontend polls a stage timeline instead of holding a request open.",-1)),e[5]||(e[5]=t("p",null,"The machinery behind that timeline is shared, not copied per feature: a run is tracked by a registry that keeps at most one run per key and remembers the current or most recent one, while each stage records its own duration and exactly one stage at a time reports a live clock. A pipeline supplies only its stage ids and two policy choices — whether runs are capped by a concurrency ceiling, and whether a finished run is evicted after a while. Heap-dump initialization is uncapped and keeps its last run indefinitely, because the work is local CPU and IO and the settings page displays that run; a pipeline that holds an external resource open for minutes would instead be capped and expire once its result is stored.",-1)),e[6]||(e[6]=t("p",null,"Most runs are queued onto the registry's own executor, which is what lets the request return. Profile initialization is the exception: it is already scheduled by whoever created the profile, and the profile can only be enabled once the run is done, so it runs on that caller's thread and the registry does the bookkeeping only. The difference is visible in one more place — a queued run has nobody waiting on it, so a failure is recorded and swallowed, while an inline one has a caller who must not go on to enable a profile that never finished building.",-1)),e[7]||(e[7]=t("p",null,'A stage can also say it did not run. Profile initialization derives trace tables only when the recording declares span-carrying event types; for an ordinary JFR recording that stage reports itself skipped rather than instantaneous, which is the difference between "there was nothing to do" and "it was fast".',-1)),e[8]||(e[8]=t("p",null,"What a run is keyed by is the feature's own business. The heap dump runs once per profile, so its key is the profile alone; a pipeline that fans out — one run per event type, say — files one run per unit and lets them drain through the shared ceiling a few at a time. Any aggregate over those runs, and any rule about how many may be in flight, belongs to the feature, not to the pipeline.",-1)),e[9]||(e[9]=t("p",null,[i("Live progress deliberately lives only in memory. A run interrupted by a restart is gone, which is truthful — the work died with the process. What survives is the terminal outcome: when a run finishes or fails, its stage timings are written to a "),t("code",null,"pipeline_runs"),i(" row in the profile database, keyed by pipeline and by what the run targeted. A batch is then simply every row of its pipeline, aggregated on read, so a page reopened later still shows what happened.")],-1)),e[10]||(e[10]=t("p",null,"The recordings list reads live progress and nothing else, which is why a recording being imported shows its stages as chips on its own row. Reaching for the stored rows there would mean opening every listed profile's database on every request, to answer a question the list is not asking: what it shows is a profile being built now.",-1))]),n(o)]))}}),H=g(w,[["__scopeId","data-v-38004b9b"]]);export{H as default};
