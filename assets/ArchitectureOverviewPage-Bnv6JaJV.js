import{D as n}from"./DocsNavFooter-C0b6Waqh.js";import{D as o}from"./DocsArchDiagram-Dwi_wUqC.js";import{D as d}from"./DocsPageHeader-CeHbfJX5.js";import{u as l}from"./useDocHeadings-Dz62a91W.js";import{i as h,o as c,e as p,h as i,a as t,g as a,f,m as u}from"./index-DXJLcJC9.js";import{_ as g}from"./_plugin-vue_export-helper-DlAUqK2U.js";const v={class:"docs-article"},y={class:"docs-content"},m=h({__name:"ArchitectureOverviewPage",setup(w){const{setHeadings:s}=l(),r=[{id:"high-level-architecture",text:"High-Level Architecture",level:2},{id:"staged-pipelines",text:"Staged Pipelines",level:2}];return c(()=>{s(r)}),(b,e)=>(u(),p("article",v,[i(d,{title:"Architecture Overview",icon:"bi bi-diagram-3"}),t("div",y,[e[0]||(e[0]=t("p",null,[a("Jeffrey consists of two applications: "),t("strong",null,"Jeffrey Microscope"),a(" (an analysis tool for visualizing and exploring JFR profiles) and "),t("strong",null,"Jeffrey Hub"),a(" (a recording collection service that manages workspaces, sessions, and live recordings). Jeffrey Microscope connects to Jeffrey Hub via gRPC to access remote workspaces, and also offers "),t("strong",null,"Recordings"),a(" for analyzing JFR files directly without a server connection.")],-1)),e[1]||(e[1]=t("h2",{id:"high-level-architecture"},"High-Level Architecture",-1)),e[2]||(e[2]=t("p",null,"Jeffrey runs as two separate applications. Jeffrey Hub collects and stores recordings from Java applications, while Jeffrey Microscope provides the analysis UI and connects to the server via gRPC:",-1)),i(o),e[3]||(e[3]=f('<h2 id="staged-pipelines" data-v-e1af65ff>Staged Pipelines</h2><p data-v-e1af65ff>Some work in Microscope takes minutes rather than milliseconds — turning a recording into a profile, initializing a heap dump, or asking the Advisor to read your source. Those requests return immediately and the frontend polls a stage timeline instead of holding a request open.</p><p data-v-e1af65ff>The machinery behind that timeline is shared, not copied per feature: a run is tracked by a registry that keeps at most one run per key and remembers the current or most recent one, while each stage records its own duration and exactly one stage at a time reports a live clock. A pipeline supplies only its stage ids and two policy choices — whether runs are capped by a concurrency ceiling, and whether a finished run is evicted after a while. Heap-dump initialization is uncapped and keeps its last run indefinitely, because the work is local CPU and IO and the settings page displays that run; an Advisor run holds a model call open for minutes, so it is capped and expires once its recommendations are stored.</p><p data-v-e1af65ff>Most runs are queued onto the registry&#39;s own executor, which is what lets the request return. Profile initialization is the exception: it is already scheduled by whoever created the profile, and the profile can only be enabled once the run is done, so it runs on that caller&#39;s thread and the registry does the bookkeeping only. The difference is visible in one more place — a queued run has nobody waiting on it, so a failure is recorded and swallowed, while an inline one has a caller who must not go on to enable a profile that never finished building.</p><p data-v-e1af65ff>A stage can also say it did not run. Profile initialization derives trace tables only when the recording declares span-carrying event types; for an ordinary JFR recording that stage reports itself skipped rather than instantaneous, which is the difference between &quot;there was nothing to do&quot; and &quot;it was fast&quot;.</p><p data-v-e1af65ff>What a run is keyed by is the feature&#39;s own business. The heap dump runs once per profile; the Advisor launches a <em data-v-e1af65ff>batch</em> that analyses every event type at once, so it files one run per type and the types drain through the shared ceiling a few at a time. The batch itself — the aggregate the Overview renders, and the rule that a profile may only have one in flight — belongs to the Advisor, not to the pipeline.</p><p data-v-e1af65ff>Live progress deliberately lives only in memory. A run interrupted by a restart is gone, which is truthful — the work died with the process. What survives is the terminal outcome: when a run finishes or fails, its stage timings are written to a <code data-v-e1af65ff>pipeline_runs</code> row in the profile database, keyed by pipeline and by what the run targeted. A batch is then simply every row of its pipeline, aggregated on read, so a page reopened later still shows what happened.</p><p data-v-e1af65ff>The recordings list reads live progress and nothing else, which is why a recording being imported shows its stages as chips on its own row. Reaching for the stored rows there would mean opening every listed profile&#39;s database on every request, to answer a question the list is not asking: what it shows is a profile being built now.</p>',8))]),i(n)]))}}),_=g(m,[["__scopeId","data-v-e1af65ff"]]);export{_ as default};
